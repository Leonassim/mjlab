# ROS (sourced globally in .zshrc for mc-rtc-superbuild work) leaks PYTHONPATH
# into every other project's Python env, including this one -- unrelated ROS
# packages (e.g. launch_testing) then get auto-loaded as pytest plugins and
# crash on missing deps (lark, etc.) that mjlab has no reason to need.
unset PYTHONPATH
